# Usamos un bucle for con range() para iterar del 1 al 10
for numero in range(1, 11):  # range(1, 11) genera los números del 1 al 10 (el último número no está incluido)
    print(f"Número: {numero}")
